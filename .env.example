DATA_MODE=synthetic
DATABASE_URL=sqlite:///./aerobust.db
PORT=8000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
MODEL_DIR=models/checkpoints
MODEL_VERSION=synthetic_bust_v1
LOG_LEVEL=INFO
# Public browser variables must avoid secrets; keep only safe frontend settings.
VITE_API_URL=

# Optional: use a managed PostgreSQL service for production deployments.
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/database
